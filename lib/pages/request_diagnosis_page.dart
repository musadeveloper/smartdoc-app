import 'package:flutter/material.dart';

class RequestDiagnosisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Request Diagnosis'),
      ),
      body: Center(
        child: Text('Request Diagnosis Page'),
      ),
    );
  }
}