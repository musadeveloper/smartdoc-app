import 'package:flutter/material.dart';

class ViewDiagnosisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Diagnosis'),
      ),
      body: Center(
        child: Text('View Diagnosis Page'),
      ),
    );
  }
}